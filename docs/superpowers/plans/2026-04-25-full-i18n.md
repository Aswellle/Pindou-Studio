# Full i18n Internationalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace every hardcoded UI string in bead-studio with `t()` calls, and fill all 4 locale files (zh-CN, en-US, ja-JP, ko-KR) with the complete key set.

**Architecture:** All text lives in `src/i18n/locales/*.json`. Components call `useTranslation()` and use `t('key')`. Template data uses English keys for category/difficulty; Gallery translates them for display. Auth errors are thrown as i18n key strings and translated at the display layer.

**Tech Stack:** react-i18next, i18next, existing 4 locale files

---

## Task 1: Rewrite all 4 locale files with full key set

**Files:**
- Rewrite: `src/i18n/locales/zh-CN.json`
- Rewrite: `src/i18n/locales/en-US.json`
- Rewrite: `src/i18n/locales/ja-JP.json`
- Rewrite: `src/i18n/locales/ko-KR.json`

- [ ] **Step 1: Write zh-CN.json**

Replace entire file with:
```json
{
  "app": { "title": "拼豆Studio", "subtitle": "新一代拼豆图纸设计工具" },
  "nav": { "canvas": "画布", "gallery": "图库", "tutorials": "教程" },
  "auth": {
    "login": "登录", "register": "注册", "logout": "退出登录",
    "email": "邮箱", "password": "密码", "confirmPassword": "确认密码",
    "welcomeBack": "欢迎回来！", "createAccount": "创建新账户",
    "noAccount": "还没有账户？", "hasAccount": "已有账户？",
    "registerNow": "立即注册", "loginNow": "立即登录",
    "processing": "处理中..."
  },
  "errors": {
    "invalidEmail": "请输入有效的邮箱地址",
    "passwordTooShort": "密码至少需要6个字符",
    "passwordMismatch": "两次输入的密码不一致"
  },
  "palette": {
    "title": "色卡",
    "brand": { "perler": "Perler (美国)", "hama": "Hama (丹麦)", "artkal": "Artkal (中国/荷兰)" },
    "search": "搜索颜色...", "recent": "最近使用", "colors": "色",
    "title2": "拼豆色卡", "expand": "展开色卡", "collapse": "收起色卡"
  },
  "canvas": {
    "size": "画布尺寸",
    "tool": { "pencil": "画笔", "eraser": "橡皮擦", "fill": "填充", "hand": "抓手" }
  },
  "tools": {
    "title": "工具", "canvasSize": "画布尺寸", "quickActions": "快捷操作",
    "import": "导入", "imageToBead": "图片转拼豆",
    "expand": "展开工具栏", "collapse": "收起工具栏",
    "clear": "清空", "undo": "撤销",
    "clearCanvas": "清空画布", "undoAction": "撤销",
    "cells": "格",
    "presets": {
      "squareGroup": "方形", "rectangleGroup": "矩形", "customGroup": "自定义",
      "smallIcon": "小图标", "standard": "标准", "large": "大图",
      "extraLarge": "超大", "huge": "巨幅", "superHuge": "超巨幅",
      "landscape": "横向", "portrait": "纵向", "custom": "自定义尺寸..."
    },
    "customSizePrompt": "请输入画布尺寸（如：87x58 或 87）：",
    "widthRange": "宽度必须在 9-200 之间",
    "heightRange": "高度必须在 9-200 之间"
  },
  "gallery": {
    "title": "图库", "subtitle": "浏览预设模板，获取创作灵感",
    "search": "搜索模板...", "allTemplates": "全部模板",
    "myFavorites": "我的收藏", "myWorks": "我的作品",
    "category": "分类：", "difficulty": "难度：",
    "categories": { "all": "全部", "animal": "动物", "food": "食物", "icon": "图标", "holiday": "节日" },
    "difficulties": { "all": "全部", "easy": "简单", "medium": "中等", "hard": "困难" },
    "noWorks": "还没有保存的作品",
    "noWorksHint": "在画布上创作后，点击导出按钮保存作品",
    "noResults": "没有找到匹配的模板",
    "noResultsHint": "尝试调整搜索条件或筛选器",
    "workName": "作品", "load": "加载", "delete": "删除",
    "myWorksSectionTitle": "我的作品"
  },
  "export": {
    "title": "导出图纸", "png": "PNG 图片", "svg": "SVG 矢量", "text": "图纸文本",
    "patternSheet": "拼豆图纸 PNG", "patternSheetSVG": "拼豆图纸 SVG",
    "exporting": "生成中"
  },
  "quantizer": {
    "title": "图片转拼豆", "dragDrop": "拖拽图片到这里", "or": "或",
    "browse": "浏览文件", "paste": "粘贴图片 (Ctrl+V)", "changeImage": "更换图片",
    "settings": "量化设置", "palette": "目标色卡", "gridSize": "画布尺寸",
    "maxColors": "目标颜色数", "colors": "色", "processing": "处理中",
    "preview": "预览", "colorsUsed": "使用颜色", "generate": "生成图纸",
    "reset": "重新上传", "applyToCanvas": "应用到画布",
    "autoRecommended": "自动 · 根据尺寸推荐",
    "longSide": "长边像素数", "actualSize": "实际尺寸", "aspectRatio": "宽高比",
    "uploadFirst": "请先上传图片",
    "widthPlaceholder": "宽", "heightPlaceholder": "高",
    "settingsChanged": "设置已变更", "regenerateHint": "点击上方按钮重新生成",
    "ditheringApplied": "已应用抖动",
    "algorithm": "算法",
    "dithering": {
      "none": "无抖动（干净边缘）",
      "floydSteinberg": "Floyd-Steinberg（渐变过渡好）",
      "ordered": "有序抖动（规律纹理）"
    },
    "algorithmHint": "抖动可产生更平滑的颜色过渡",
    "quality": "处理质量",
    "qualityModes": { "high": "高质量（推荐，~3-5秒）", "fast": "快速预览（~1秒）" },
    "qualityHint": "高质量模式使用更多次迭代优化配色",
    "removeBackground": "自动移除背景",
    "removeBackgroundHint": "检测纯色背景并标记为透明（适合有背景的截图）",
    "brightness": "亮度", "contrast": "对比度",
    "colorHint": "简单图 12-24 · 插画 28-48 · 复杂图 48-72 · 极高细节 72+",
    "unsavedTitle": "有未保存的更改",
    "unsavedBody": "您有已生成但未应用的图纸。关闭后将丢失。",
    "continueEditing": "继续编辑", "discardClose": "放弃并关闭",
    "processingAlgo": "使用 CIEDE2000 精准配色算法",
    "presets": {
      "29x29": "29 × 29（标准）", "57x57": "57 × 57（大图）",
      "114x114": "114 × 114（超大）", "140x140": "140 × 140（专业站）",
      "57x29": "57 × 29（横幅）", "29x57": "29 × 57（竖幅）",
      "aspect": "按原图比例（指定长边）", "custom": "自定义宽×高"
    }
  },
  "tutorials": {
    "title": "教程目录", "markAllRead": "全部标为已读", "reset": "重置",
    "steps": "操作步骤", "tips": "小贴士", "diagram": "示意图",
    "noSelection": "选择一个教程开始学习",
    "prev": "上一篇：", "next": "下一篇：", "noMore": "没有了"
  },
  "stats": { "noData": "暂无数据", "beads": "颗珠子", "colors": "种颜色" },
  "common": {
    "save": "保存", "cancel": "取消", "clear": "清空", "undo": "撤销",
    "redo": "重做", "delete": "删除", "edit": "编辑", "confirm": "确认",
    "loading": "加载中...", "error": "错误", "success": "成功",
    "close": "关闭", "back": "返回", "next": "下一步", "previous": "上一步"
  }
}
```

- [ ] **Step 2: Write en-US.json**

Replace entire file with:
```json
{
  "app": { "title": "Bead Studio", "subtitle": "Next-generation bead pattern design tool" },
  "nav": { "canvas": "Canvas", "gallery": "Gallery", "tutorials": "Tutorials" },
  "auth": {
    "login": "Login", "register": "Register", "logout": "Logout",
    "email": "Email", "password": "Password", "confirmPassword": "Confirm Password",
    "welcomeBack": "Welcome back!", "createAccount": "Create a new account",
    "noAccount": "Don't have an account?", "hasAccount": "Already have an account?",
    "registerNow": "Register now", "loginNow": "Login now",
    "processing": "Processing..."
  },
  "errors": {
    "invalidEmail": "Please enter a valid email address",
    "passwordTooShort": "Password must be at least 6 characters",
    "passwordMismatch": "Passwords do not match"
  },
  "palette": {
    "title": "Color Palette",
    "brand": { "perler": "Perler (USA)", "hama": "Hama (Denmark)", "artkal": "Artkal (China/Netherlands)" },
    "search": "Search colors...", "recent": "Recently used", "colors": "colors",
    "title2": "Bead Palette", "expand": "Expand palette", "collapse": "Collapse palette"
  },
  "canvas": {
    "size": "Canvas Size",
    "tool": { "pencil": "Pencil", "eraser": "Eraser", "fill": "Fill", "hand": "Hand" }
  },
  "tools": {
    "title": "Tools", "canvasSize": "Canvas Size", "quickActions": "Quick Actions",
    "import": "Import", "imageToBead": "Image to Beads",
    "expand": "Expand toolbar", "collapse": "Collapse toolbar",
    "clear": "Clear", "undo": "Undo",
    "clearCanvas": "Clear Canvas", "undoAction": "Undo",
    "cells": "cells",
    "presets": {
      "squareGroup": "Square", "rectangleGroup": "Rectangle", "customGroup": "Custom",
      "smallIcon": "Small Icon", "standard": "Standard", "large": "Large",
      "extraLarge": "Extra Large", "huge": "Huge", "superHuge": "Super Huge",
      "landscape": "Landscape", "portrait": "Portrait", "custom": "Custom size..."
    },
    "customSizePrompt": "Enter canvas size (e.g. 87x58 or 87):",
    "widthRange": "Width must be between 9 and 200",
    "heightRange": "Height must be between 9 and 200"
  },
  "gallery": {
    "title": "Gallery", "subtitle": "Browse preset templates for inspiration",
    "search": "Search templates...", "allTemplates": "All Templates",
    "myFavorites": "My Favorites", "myWorks": "My Works",
    "category": "Category:", "difficulty": "Difficulty:",
    "categories": { "all": "All", "animal": "Animals", "food": "Food", "icon": "Icons", "holiday": "Holidays" },
    "difficulties": { "all": "All", "easy": "Easy", "medium": "Medium", "hard": "Hard" },
    "noWorks": "No saved works yet",
    "noWorksHint": "Create something on the canvas, then click export to save",
    "noResults": "No matching templates found",
    "noResultsHint": "Try adjusting your search or filters",
    "workName": "Work", "load": "Load", "delete": "Delete",
    "myWorksSectionTitle": "My Works"
  },
  "export": {
    "title": "Export Pattern", "png": "PNG Image", "svg": "SVG Vector", "text": "Pattern Text",
    "patternSheet": "Bead Sheet PNG", "patternSheetSVG": "Bead Sheet SVG",
    "exporting": "Generating"
  },
  "quantizer": {
    "title": "Image to Beads", "dragDrop": "Drop image here", "or": "or",
    "browse": "Browse Files", "paste": "Paste Image (Ctrl+V)", "changeImage": "Change Image",
    "settings": "Quantize Settings", "palette": "Target Palette", "gridSize": "Canvas Size",
    "maxColors": "Max Colors", "colors": "colors", "processing": "Processing",
    "preview": "Preview", "colorsUsed": "Colors Used", "generate": "Generate Pattern",
    "reset": "Reset", "applyToCanvas": "Apply to Canvas",
    "autoRecommended": "Auto · Recommended for size",
    "longSide": "Long side (cells)", "actualSize": "Actual size", "aspectRatio": "aspect ratio",
    "uploadFirst": "Upload an image first",
    "widthPlaceholder": "W", "heightPlaceholder": "H",
    "settingsChanged": "Settings changed", "regenerateHint": "Click Generate to update",
    "ditheringApplied": "Dithering applied",
    "algorithm": "Algorithm",
    "dithering": {
      "none": "No dithering (clean edges)",
      "floydSteinberg": "Floyd-Steinberg (smooth gradients)",
      "ordered": "Ordered dithering (regular texture)"
    },
    "algorithmHint": "Dithering creates smoother color transitions",
    "quality": "Processing Quality",
    "qualityModes": { "high": "High quality (recommended, ~3-5s)", "fast": "Fast preview (~1s)" },
    "qualityHint": "High quality uses more iterations for better color matching",
    "removeBackground": "Auto remove background",
    "removeBackgroundHint": "Detects solid backgrounds and marks them transparent",
    "brightness": "Brightness", "contrast": "Contrast",
    "colorHint": "Simple 12-24 · Illustration 28-48 · Complex 48-72 · High detail 72+",
    "unsavedTitle": "Unsaved Changes",
    "unsavedBody": "You have a generated pattern that hasn't been applied. It will be lost if you close.",
    "continueEditing": "Continue Editing", "discardClose": "Discard & Close",
    "processingAlgo": "Using CIEDE2000 color matching",
    "presets": {
      "29x29": "29 × 29 (Standard)", "57x57": "57 × 57 (Large)",
      "114x114": "114 × 114 (Extra Large)", "140x140": "140 × 140 (Professional)",
      "57x29": "57 × 29 (Landscape)", "29x57": "29 × 57 (Portrait)",
      "aspect": "Match image aspect ratio", "custom": "Custom width × height"
    }
  },
  "tutorials": {
    "title": "Tutorial Contents", "markAllRead": "Mark all as read", "reset": "Reset",
    "steps": "Steps", "tips": "Tips", "diagram": "Diagram",
    "noSelection": "Select a tutorial from the left to begin",
    "prev": "Previous: ", "next": "Next: ", "noMore": "None"
  },
  "stats": { "noData": "No data yet", "beads": "beads", "colors": "colors" },
  "common": {
    "save": "Save", "cancel": "Cancel", "clear": "Clear", "undo": "Undo",
    "redo": "Redo", "delete": "Delete", "edit": "Edit", "confirm": "Confirm",
    "loading": "Loading...", "error": "Error", "success": "Success",
    "close": "Close", "back": "Back", "next": "Next", "previous": "Previous"
  }
}
```

- [ ] **Step 3: Write ja-JP.json** (merge missing keys into existing)

Replace entire file with (preserves all existing translations, adds missing keys):
```json
{
  "app": { "title": "ビードスタジオ", "subtitle": "次世代ビードパターンデザイン工具" },
  "nav": { "canvas": "キャンバス", "gallery": "ギャラリー", "tutorials": "チュートリアル" },
  "auth": {
    "login": "ログイン", "register": "登録", "logout": "ログアウト",
    "email": "メールアドレス", "password": "パスワード", "confirmPassword": "パスワードを確認",
    "welcomeBack": "おかえりなさい！", "createAccount": "新規アカウント作成",
    "noAccount": "アカウントをお持ちでない方？", "hasAccount": "すでにアカウントをお持ちの方？",
    "registerNow": "今すぐ登録", "loginNow": "今すぐログイン",
    "processing": "処理中..."
  },
  "errors": {
    "invalidEmail": "有効なメールアドレスを入力してください",
    "passwordTooShort": "パスワードは6文字以上必要です",
    "passwordMismatch": "パスワードが一致しません"
  },
  "palette": {
    "title": "カラーパレット",
    "brand": { "perler": "Perler (アメリカ)", "hama": "Hama (デンマーク)", "artkal": "Artkal (中国/オランダ)" },
    "search": "カラーを検索...", "recent": "最近使用", "colors": "色",
    "title2": "ビードパレット", "expand": "パレットを展開", "collapse": "パレットを折りたたむ"
  },
  "canvas": {
    "size": "キャンバスサイズ",
    "tool": { "pencil": "鉛筆", "eraser": "消しゴム", "fill": "塗りつぶし", "hand": "移動" }
  },
  "tools": {
    "title": "工具", "canvasSize": "キャンバスサイズ", "quickActions": "クイックアクション",
    "import": "インポート", "imageToBead": "画像をビードに",
    "expand": "ツールバーを展開", "collapse": "ツールバーを折りたたむ",
    "clear": "クリア", "undo": "元に戻す",
    "clearCanvas": "キャンバスをクリア", "undoAction": "元に戻す",
    "cells": "セル",
    "presets": {
      "squareGroup": "正方形", "rectangleGroup": "長方形", "customGroup": "カスタム",
      "smallIcon": "小アイコン", "standard": "標準", "large": "大",
      "extraLarge": "特大", "huge": "超大", "superHuge": "超々大",
      "landscape": "横向き", "portrait": "縦向き", "custom": "カスタムサイズ..."
    },
    "customSizePrompt": "キャンバスサイズを入力（例：87x58 または 87）：",
    "widthRange": "幅は9から200の間である必要があります",
    "heightRange": "高さは9から200の間である必要があります"
  },
  "gallery": {
    "title": "テンプレートギャラリー", "subtitle": "テンプレートを閲覧してインスピレーションを得ましょう",
    "search": "テンプレートを検索...", "allTemplates": "すべてのテンプレート",
    "myFavorites": "お気に入り", "myWorks": "マイ作品",
    "category": "カテゴリ：", "difficulty": "難易度：",
    "categories": { "all": "すべて", "animal": "動物", "food": "食べ物", "icon": "アイコン", "holiday": "祝日" },
    "difficulties": { "all": "すべて", "easy": "簡単", "medium": "中級", "hard": "上級" },
    "noWorks": "保存された作品がありません",
    "noWorksHint": "キャンバスで作品を作ってからエクスポートして保存しましょう",
    "noResults": "一致するテンプレートが見つかりません",
    "noResultsHint": "検索条件やフィルターを調整してみてください",
    "workName": "作品", "load": "読み込む", "delete": "削除",
    "myWorksSectionTitle": "マイ作品"
  },
  "export": {
    "title": "エクスポート", "png": "PNG 画像", "svg": "SVG ベクター", "text": "パターン テキスト",
    "patternSheet": "ビードシート PNG", "patternSheetSVG": "ビードシート SVG",
    "exporting": "生成中"
  },
  "quantizer": {
    "title": "画像をビードに", "dragDrop": "画像をここにドロップ", "or": "または",
    "browse": "ファイルを参照", "paste": "画像を貼り付け (Ctrl+V)", "changeImage": "画像を変更",
    "settings": "量子化設定", "palette": "対象パレット", "gridSize": "キャンバスサイズ",
    "maxColors": "最大カラー数", "colors": "色", "processing": "処理中",
    "preview": "プレビュー", "colorsUsed": "使用カラー", "generate": "パターンを生成",
    "reset": "リセット", "applyToCanvas": "キャンバスに適用",
    "autoRecommended": "自動 · サイズに基づく推奨",
    "longSide": "長辺（セル）", "actualSize": "実際のサイズ", "aspectRatio": "アスペクト比",
    "uploadFirst": "先に画像をアップロードしてください",
    "widthPlaceholder": "幅", "heightPlaceholder": "高さ",
    "settingsChanged": "設定が変更されました", "regenerateHint": "生成ボタンをクリックして更新",
    "ditheringApplied": "ディザリング適用済み",
    "algorithm": "アルゴリズム",
    "dithering": {
      "none": "ディザリングなし（クリーンなエッジ）",
      "floydSteinberg": "Floyd-Steinberg（スムーズなグラデーション）",
      "ordered": "秩序ディザリング（規則的なテクスチャ）"
    },
    "algorithmHint": "ディザリングはよりスムーズなカラー遷移を作ります",
    "quality": "処理品質",
    "qualityModes": { "high": "高品質（推奨、~3-5秒）", "fast": "高速プレビュー（~1秒）" },
    "qualityHint": "高品質モードはより多くのイテレーションでカラーマッチングを改善します",
    "removeBackground": "背景を自動削除",
    "removeBackgroundHint": "単色背景を検出して透明にマークします",
    "brightness": "明るさ", "contrast": "コントラスト",
    "colorHint": "シンプル 12-24 · イラスト 28-48 · 複雑 48-72 · 高精細 72+",
    "unsavedTitle": "未保存の変更があります",
    "unsavedBody": "生成されたパターンがまだ適用されていません。閉じると失われます。",
    "continueEditing": "編集を続ける", "discardClose": "破棄して閉じる",
    "processingAlgo": "CIEDE2000カラーマッチングを使用中",
    "presets": {
      "29x29": "29 × 29（標準）", "57x57": "57 × 57（大）",
      "114x114": "114 × 114（特大）", "140x140": "140 × 140（プロ）",
      "57x29": "57 × 29（横向き）", "29x57": "29 × 57（縦向き）",
      "aspect": "画像のアスペクト比に合わせる", "custom": "カスタム幅×高さ"
    }
  },
  "tutorials": {
    "title": "チュートリアル目次", "markAllRead": "すべて既読にする", "reset": "リセット",
    "steps": "手順", "tips": "ヒント", "diagram": "図",
    "noSelection": "左からチュートリアルを選択してください",
    "prev": "前へ：", "next": "次へ：", "noMore": "なし"
  },
  "stats": { "noData": "データなし", "beads": "個のビード", "colors": "色" },
  "common": {
    "save": "保存", "cancel": "キャンセル", "clear": "クリア", "undo": "元に戻す",
    "redo": "やり直し", "delete": "削除", "edit": "編集", "confirm": "確認",
    "loading": "読み込み中...", "error": "エラー", "success": "成功",
    "close": "閉じる", "back": "戻る", "next": "次へ", "previous": "前へ"
  }
}
```

- [ ] **Step 4: Write ko-KR.json** (fix Chinese remnants, add all missing keys)

Replace entire file with:
```json
{
  "app": { "title": "비드 스튜디오", "subtitle": "차세대 비드 패턴 디자인 도구" },
  "nav": { "canvas": "캔버스", "gallery": "갤러리", "tutorials": "튜토리얼" },
  "auth": {
    "login": "로그인", "register": "회원가입", "logout": "로그아웃",
    "email": "이메일", "password": "비밀번호", "confirmPassword": "비밀번호 확인",
    "welcomeBack": "다시 오셨군요!", "createAccount": "새 계정 만들기",
    "noAccount": "계정이 없으신가요?", "hasAccount": "이미 계정이 있으신가요?",
    "registerNow": "지금 가입", "loginNow": "지금 로그인",
    "processing": "처리 중..."
  },
  "errors": {
    "invalidEmail": "유효한 이메일 주소를 입력하세요",
    "passwordTooShort": "비밀번호는 6자 이상이어야 합니다",
    "passwordMismatch": "비밀번호가 일치하지 않습니다"
  },
  "palette": {
    "title": "색상 팔레트",
    "brand": { "perler": "Perler (미국)", "hama": "Hama (덴마크)", "artkal": "Artkal (중국/네덜란드)" },
    "search": "색상 검색...", "recent": "최근 사용", "colors": "색",
    "title2": "비드 팔레트", "expand": "팔레트 펼치기", "collapse": "팔레트 접기"
  },
  "canvas": {
    "size": "캔버스 크기",
    "tool": { "pencil": "연필", "eraser": "지우개", "fill": "채우기", "hand": "이동" }
  },
  "tools": {
    "title": "도구", "canvasSize": "캔버스 크기", "quickActions": "빠른 작업",
    "import": "가져오기", "imageToBead": "이미지를 비드로",
    "expand": "도구 모음 펼치기", "collapse": "도구 모음 접기",
    "clear": "지우기", "undo": "실행 취소",
    "clearCanvas": "캔버스 지우기", "undoAction": "실행 취소",
    "cells": "칸",
    "presets": {
      "squareGroup": "정사각형", "rectangleGroup": "직사각형", "customGroup": "사용자 지정",
      "smallIcon": "작은 아이콘", "standard": "표준", "large": "크게",
      "extraLarge": "매우 크게", "huge": "초대형", "superHuge": "초초대형",
      "landscape": "가로형", "portrait": "세로형", "custom": "사용자 정의 크기..."
    },
    "customSizePrompt": "캔버스 크기를 입력하세요 (예: 87x58 또는 87):",
    "widthRange": "너비는 9에서 200 사이여야 합니다",
    "heightRange": "높이는 9에서 200 사이여야 합니다"
  },
  "gallery": {
    "title": "갤러리", "subtitle": "사전 설정 템플릿을 둘러보고 영감을 얻으세요",
    "search": "템플릿 검색...", "allTemplates": "모든 템플릿",
    "myFavorites": "즐겨찾기", "myWorks": "내 작품",
    "category": "카테고리:", "difficulty": "난이도:",
    "categories": { "all": "전체", "animal": "동물", "food": "음식", "icon": "아이콘", "holiday": "명절" },
    "difficulties": { "all": "전체", "easy": "쉬움", "medium": "보통", "hard": "어려움" },
    "noWorks": "저장된 작품이 없습니다",
    "noWorksHint": "캔버스에서 작품을 만든 후 내보내기 버튼을 클릭하세요",
    "noResults": "일치하는 템플릿이 없습니다",
    "noResultsHint": "검색어나 필터를 조정해 보세요",
    "workName": "작품", "load": "불러오기", "delete": "삭제",
    "myWorksSectionTitle": "내 작품"
  },
  "export": {
    "title": "패턴 내보내기", "png": "PNG 이미지", "svg": "SVG 벡터", "text": "패턴 텍스트",
    "patternSheet": "비드 시트 PNG", "patternSheetSVG": "비드 시트 SVG",
    "exporting": "생성 중"
  },
  "quantizer": {
    "title": "이미지를 비드로", "dragDrop": "이미지를 여기에 드롭", "or": "또는",
    "browse": "파일 찾아보기", "paste": "이미지 붙여넣기 (Ctrl+V)", "changeImage": "이미지 변경",
    "settings": "양자화 설정", "palette": "대상 팔레트", "gridSize": "캔버스 크기",
    "maxColors": "최대 색상 수", "colors": "색", "processing": "처리 중",
    "preview": "미리보기", "colorsUsed": "사용 색상", "generate": "패턴 생성",
    "reset": "다시 업로드", "applyToCanvas": "캔버스에 적용",
    "autoRecommended": "자동 · 크기에 따라 추천",
    "longSide": "긴 쪽 (칸)", "actualSize": "실제 크기", "aspectRatio": "가로세로 비율",
    "uploadFirst": "먼저 이미지를 업로드하세요",
    "widthPlaceholder": "너비", "heightPlaceholder": "높이",
    "settingsChanged": "설정이 변경됨", "regenerateHint": "생성 버튼을 클릭하여 업데이트",
    "ditheringApplied": "디더링 적용됨",
    "algorithm": "알고리즘",
    "dithering": {
      "none": "디더링 없음 (깔끔한 경계)",
      "floydSteinberg": "Floyd-Steinberg (부드러운 그라데이션)",
      "ordered": "순서 디더링 (규칙적 질감)"
    },
    "algorithmHint": "디더링은 더 부드러운 색상 전환을 만듭니다",
    "quality": "처리 품질",
    "qualityModes": { "high": "고품질 (권장, ~3-5초)", "fast": "빠른 미리보기 (~1초)" },
    "qualityHint": "고품질 모드는 더 많은 반복으로 색상 매칭을 개선합니다",
    "removeBackground": "배경 자동 제거",
    "removeBackgroundHint": "단색 배경을 감지하여 투명으로 표시합니다",
    "brightness": "밝기", "contrast": "대비",
    "colorHint": "단순 12-24 · 일러스트 28-48 · 복잡 48-72 · 고세밀 72+",
    "unsavedTitle": "저장되지 않은 변경 사항",
    "unsavedBody": "생성된 패턴이 적용되지 않았습니다. 닫으면 사라집니다.",
    "continueEditing": "계속 편집", "discardClose": "버리고 닫기",
    "processingAlgo": "CIEDE2000 색상 매칭 사용 중",
    "presets": {
      "29x29": "29 × 29 (표준)", "57x57": "57 × 57 (크게)",
      "114x114": "114 × 114 (매우 크게)", "140x140": "140 × 140 (전문가)",
      "57x29": "57 × 29 (가로형)", "29x57": "29 × 57 (세로형)",
      "aspect": "이미지 비율에 맞추기", "custom": "사용자 정의 너비 × 높이"
    }
  },
  "tutorials": {
    "title": "튜토리얼 목차", "markAllRead": "모두 읽음으로 표시", "reset": "초기화",
    "steps": "단계", "tips": "팁", "diagram": "다이어그램",
    "noSelection": "왼쪽에서 튜토리얼을 선택하세요",
    "prev": "이전: ", "next": "다음: ", "noMore": "없음"
  },
  "stats": { "noData": "데이터 없음", "beads": "개 비드", "colors": "가지 색상" },
  "common": {
    "save": "저장", "cancel": "취소", "clear": "지우기", "undo": "실행 취소",
    "redo": "다시 실행", "delete": "삭제", "edit": "편집", "confirm": "확인",
    "loading": "로딩 중...", "error": "오류", "success": "성공",
    "close": "닫기", "back": "뒤로", "next": "다음", "previous": "이전"
  }
}
```

- [ ] **Step 5: Commit**
```bash
git add src/i18n/locales/
git commit -m "i18n: expand all 4 locale files with complete key set"
```

---

## Task 2: Convert templates.js category/difficulty to English keys

**Files:**
- Modify: `src/data/templates.js`

- [ ] **Step 1: Replace Chinese category/difficulty values with English keys**

In `src/data/templates.js`, make these replacements (use replace_all):
- `difficulty: '简单'` → `difficulty: 'easy'`
- `difficulty: '中等'` → `difficulty: 'medium'`
- `difficulty: '困难'` → `difficulty: 'hard'`
- `category: '动物'` → `category: 'animal'`
- `category: '食物'` → `category: 'food'`
- `category: '图标'` → `category: 'icon'`
- `category: '节日'` → `category: 'holiday'`

Also replace the CATEGORIES and DIFFICULTIES export at the bottom:
```js
export const CATEGORY_KEYS = ['all', 'animal', 'food', 'icon', 'holiday']
export const DIFFICULTY_KEYS = ['all', 'easy', 'medium', 'hard']
```
(Remove the old `CATEGORIES` and `DIFFICULTIES` exports entirely.)

- [ ] **Step 2: Commit**
```bash
git add src/data/templates.js
git commit -m "i18n: convert template category/difficulty to English keys"
```

---

## Task 3: Update useAuth.js — throw i18n error codes

**Files:**
- Modify: `src/hooks/useAuth.js`

- [ ] **Step 1: Replace Chinese error messages with i18n key strings**

Change all three `reject(new Error(...))` calls:
```js
// line ~27
reject(new Error('errors.invalidEmail'))
// line ~30
reject(new Error('errors.passwordTooShort'))
// line ~51
reject(new Error('errors.invalidEmail'))
// line ~54
reject(new Error('errors.passwordTooShort'))
// line ~57
reject(new Error('errors.passwordMismatch'))
```

- [ ] **Step 2: Commit**
```bash
git add src/hooks/useAuth.js
git commit -m "i18n: throw error code keys from useAuth instead of Chinese strings"
```

---

## Task 4: Update AuthModal.jsx

**Files:**
- Modify: `src/components/AuthModal.jsx`

- [ ] **Step 1: Add useTranslation import and replace all hardcoded text**

Replace the entire component file content with:
```jsx
import { useState } from 'react'
import { useTranslation } from 'react-i18next'

export default function AuthModal({ mode, onClose, onLogin, onRegister, onSwitchMode }) {
  const { t } = useTranslation()
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [confirmPassword, setConfirmPassword] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  const isLogin = mode === 'login'

  const handleSubmit = async (e) => {
    e.preventDefault()
    setError('')
    setLoading(true)
    try {
      if (isLogin) {
        await onLogin(email, password)
      } else {
        await onRegister(email, password, confirmPassword)
      }
      onClose()
    } catch (err) {
      setError(err.message)
    } finally {
      setLoading(false)
    }
  }

  const validateEmail = (email) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)

  return (
    <div className="modal-overlay" onClick={onClose}>
      <div className="modal-content" onClick={(e) => e.stopPropagation()}>
        <button className="close-btn" onClick={onClose}>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
            <line x1="18" y1="6" x2="6" y2="18"/>
            <line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        </button>

        <div className="auth-header">
          <h2>{isLogin ? t('auth.login') : t('auth.register')}</h2>
          <p>{isLogin ? t('auth.welcomeBack') : t('auth.createAccount')}</p>
        </div>

        <form onSubmit={handleSubmit}>
          <div className="form-group">
            <label htmlFor="email">{t('auth.email')}</label>
            <input
              id="email"
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              placeholder="your@email.com"
              className={error && !validateEmail(email) ? 'error' : ''}
              required
            />
            {error && !validateEmail(email) && (
              <span className="error-message">{t('errors.invalidEmail')}</span>
            )}
          </div>

          <div className="form-group">
            <label htmlFor="password">{t('auth.password')}</label>
            <input
              id="password"
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              placeholder="••••••••"
              className={error && password.length < 6 ? 'error' : ''}
              required
            />
            {password.length > 0 && password.length < 6 && (
              <span className="hint">{t('errors.passwordTooShort')}</span>
            )}
          </div>

          {!isLogin && (
            <div className="form-group">
              <label htmlFor="confirmPassword">{t('auth.confirmPassword')}</label>
              <input
                id="confirmPassword"
                type="password"
                value={confirmPassword}
                onChange={(e) => setConfirmPassword(e.target.value)}
                placeholder="••••••••"
                className={error && confirmPassword !== password ? 'error' : ''}
                required
              />
              {confirmPassword && confirmPassword !== password && (
                <span className="error-message">{t('errors.passwordMismatch')}</span>
              )}
            </div>
          )}

          {error && (
            <div className="error-banner">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
                <circle cx="12" cy="12" r="10"/>
                <line x1="12" y1="8" x2="12" y2="12"/>
                <line x1="12" y1="16" x2="12.01" y2="16"/>
              </svg>
              {t(error, error)}
            </div>
          )}

          <button
            type="submit"
            className="btn btn-primary btn-full"
            disabled={loading || (!isLogin && password !== confirmPassword)}
          >
            {loading ? t('auth.processing') : (isLogin ? t('auth.login') : t('auth.register'))}
          </button>
        </form>

        <div className="auth-footer">
          {isLogin ? (
            <>
              {t('auth.noAccount')}
              <button className="link-btn" onClick={() => onSwitchMode('register')}>
                {t('auth.registerNow')}
              </button>
            </>
          ) : (
            <>
              {t('auth.hasAccount')}
              <button className="link-btn" onClick={() => onSwitchMode('login')}>
                {t('auth.loginNow')}
              </button>
            </>
          )}
        </div>

        <style>{`
          .close-btn {
            position: absolute; top: 16px; right: 16px; padding: 4px;
            border-radius: 4px; color: var(--text-muted); transition: all 0.2s;
          }
          .close-btn:hover { color: var(--text-primary); background: var(--bg-secondary); }
          .auth-header { text-align: center; margin-bottom: 24px; }
          .auth-header h2 { font-size: 24px; margin-bottom: 4px; }
          .auth-header p { color: var(--text-muted); font-size: 14px; }
          .error-banner {
            display: flex; align-items: center; gap: 8px; padding: 12px;
            background: #FFF0F0; border: 1px solid #FFD0D0; border-radius: 6px;
            color: var(--error); font-size: 13px; margin-bottom: 16px;
          }
          .auth-footer {
            text-align: center; margin-top: 24px; padding-top: 24px;
            border-top: 1px solid var(--border-color); font-size: 14px;
            color: var(--text-secondary);
          }
          .link-btn {
            color: var(--accent); font-weight: 600; margin-left: 4px;
            background: none; border: none; cursor: pointer;
          }
          .link-btn:hover { text-decoration: underline; }
        `}</style>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**
```bash
git add src/components/AuthModal.jsx
git commit -m "i18n: fully internationalize AuthModal"
```

---

## Task 5: Update Tools.jsx

**Files:**
- Modify: `src/components/Tools.jsx`

- [ ] **Step 1: Add useTranslation import at top of file**

After `import { useState, useEffect } from 'react'` add:
```js
import { useTranslation } from 'react-i18next'
```

- [ ] **Step 2: Add t() call inside component and update handlePresetChange prompts**

Inside the `Tools` component function body, add after the existing variable declarations:
```js
const { t } = useTranslation()
```

Replace the `prompt` call:
```js
const input = prompt(t('tools.customSizePrompt'), `${currentWidth}x${currentHeight}`)
```

Replace the two `alert` calls:
```js
alert(t('tools.widthRange'))
// ...
alert(t('tools.heightRange'))
```

- [ ] **Step 3: Replace all hardcoded JSX text**

Replace the drawer toggle `title` attributes:
```jsx
title={collapsed ? t('tools.expand') : t('tools.collapse')}
```

Replace the tools title:
```jsx
<h3 className="tools-title">{t('tools.title')}</h3>
```

Replace tool button titles:
```jsx
title={t('canvas.tool.pencil')}
// ...
title={t('canvas.tool.eraser')}
// ...
title={t('canvas.tool.fill')}
// ...
title={`${t('canvas.tool.hand')}`}
```

Replace canvas size label:
```jsx
<label className="tool-label">{t('tools.canvasSize')}</label>
```

Replace the `<select>` option groups and options:
```jsx
<optgroup label={`── ${t('tools.presets.squareGroup')} ──`}>
  <option value="29">29×29 {t('tools.presets.smallIcon')}</option>
  <option value="57">57×57 {t('tools.presets.standard')}</option>
  <option value="87">87×87 {t('tools.presets.large')}</option>
  <option value="114">114×114 {t('tools.presets.extraLarge')}</option>
  <option value="140">140×140 {t('tools.presets.huge')}</option>
  <option value="170">170×170 {t('tools.presets.superHuge')}</option>
</optgroup>
<optgroup label={`── ${t('tools.presets.rectangleGroup')} ──`}>
  <option value="57x29">57×29 {t('tools.presets.landscape')}</option>
  <option value="87x58">87×58 {t('tools.presets.landscape')}</option>
  <option value="114x87">114×87 {t('tools.presets.landscape')}</option>
  <option value="140x105">140×105 {t('tools.presets.landscape')}</option>
  <option value="170x115">170×115 {t('tools.presets.landscape')}</option>
  <option value="29x57">29×57 {t('tools.presets.portrait')}</option>
  <option value="58x87">58×87 {t('tools.presets.portrait')}</option>
  <option value="87x114">87×114 {t('tools.presets.portrait')}</option>
  <option value="105x140">105×140 {t('tools.presets.portrait')}</option>
  <option value="115x170">115×170 {t('tools.presets.portrait')}</option>
</optgroup>
<optgroup label={`── ${t('tools.presets.customGroup')} ──`}>
  <option value="custom">{t('tools.presets.custom')}</option>
</optgroup>
```

Replace the current-size-info text:
```jsx
<div className="current-size-info">
  {currentWidth} × {currentHeight}（{currentWidth * currentHeight} {t('tools.cells')}）
</div>
```

Replace quick actions label and buttons:
```jsx
<label className="tool-label">{t('tools.quickActions')}</label>
// ...
<button className="action-btn" onClick={onClear} title={t('tools.clearCanvas')}>
  ...
  {t('tools.clear')}
</button>
<button className="action-btn" onClick={onUndo} disabled={!canUndo} title={t('tools.undoAction')}>
  ...
  {t('tools.undo')}
</button>
```

Replace import label and button:
```jsx
<label className="tool-label">{t('tools.import')}</label>
<button className="action-btn quantizer-btn" onClick={onOpenQuantizer} title={t('tools.imageToBead')}>
  ...
  {t('tools.imageToBead')}
</button>
```

- [ ] **Step 4: Commit**
```bash
git add src/components/Tools.jsx
git commit -m "i18n: fully internationalize Tools sidebar"
```

---

## Task 6: Update Gallery.jsx

**Files:**
- Modify: `src/components/Gallery.jsx`

- [ ] **Step 1: Add useTranslation; update imports to use CATEGORY_KEYS/DIFFICULTY_KEYS**

Replace:
```js
import { TEMPLATES, CATEGORIES, DIFFICULTIES } from '../data/templates'
```
With:
```js
import { useTranslation } from 'react-i18next'
import { TEMPLATES, CATEGORY_KEYS, DIFFICULTY_KEYS } from '../data/templates'
```

- [ ] **Step 2: Add t(), update initial state, update filter logic, update getDifficultyColor**

Add inside component: `const { t } = useTranslation()`

Change initial state:
```js
const [selectedCategory, setSelectedCategory] = useState('all')
const [selectedDifficulty, setSelectedDifficulty] = useState('all')
```

Update filter logic:
```js
const matchesCategory = selectedCategory === 'all' || template.category === selectedCategory
const matchesDifficulty = selectedDifficulty === 'all' || template.difficulty === selectedDifficulty
```

Update getDifficultyColor:
```js
const getDifficultyColor = (difficulty) => {
  switch (difficulty) {
    case 'easy': return '#4CAF50'
    case 'medium': return '#FF9800'
    case 'hard': return '#E53935'
    default: return '#999'
  }
}
```

- [ ] **Step 3: Replace all hardcoded JSX text**

Replace header:
```jsx
<h1 className="gallery-title">{t('gallery.title')}</h1>
<p className="gallery-subtitle">{t('gallery.subtitle')}</p>
```

Replace search placeholder:
```jsx
placeholder={t('gallery.search')}
```

Replace filter tabs:
```jsx
<button ... onClick={() => { setShowFavorites(false); setShowMyWorks(false) }}>
  {t('gallery.allTemplates')}
</button>
<button ... onClick={() => { setShowFavorites(!showFavorites); setShowMyWorks(false) }}>
  {t('gallery.myFavorites')} ({favorites.length})
</button>
<button ... onClick={() => { setShowMyWorks(!showMyWorks); setShowFavorites(false) }}>
  {t('gallery.myWorks')} ({savedWorks.length})
</button>
```

Replace category bar:
```jsx
<span className="category-label">{t('gallery.category')}</span>
{CATEGORY_KEYS.map(cat => (
  <button key={cat} className={`category-btn ${selectedCategory === cat ? 'active' : ''}`}
    onClick={() => setSelectedCategory(cat)}>
    {t(`gallery.categories.${cat}`)}
  </button>
))}
// ...
<span className="category-label">{t('gallery.difficulty')}</span>
{DIFFICULTY_KEYS.map(diff => (
  <button key={diff} className={`difficulty-btn ${selectedDifficulty === diff ? 'active' : ''}`}
    onClick={() => setSelectedDifficulty(diff)}
    style={{ '--diff-color': getDifficultyColor(diff) }}>
    {t(`gallery.difficulties.${diff}`)}
  </button>
))}
```

Replace My Works section:
```jsx
<h2 className="section-title">{t('gallery.myWorksSectionTitle')}</h2>
// empty state:
<p>{t('gallery.noWorks')}</p>
<span>{t('gallery.noWorksHint')}</span>
// work card:
<span className="work-name">{t('gallery.workName')} {index + 1}</span>
// buttons:
<button className="work-btn load" onClick={...}>{t('gallery.load')}</button>
<button className="work-btn delete" onClick={...}>{t('gallery.delete')}</button>
```

Replace empty state for templates:
```jsx
<p>{t('gallery.noResults')}</p>
<span>{t('gallery.noResultsHint')}</span>
```

Replace template difficulty/category display:
```jsx
<span className="template-difficulty" style={{ '--diff-color': getDifficultyColor(template.difficulty) }}>
  {t(`gallery.difficulties.${template.difficulty}`)}
</span>
<span className="template-category">{t(`gallery.categories.${template.category}`)}</span>
```

- [ ] **Step 4: Commit**
```bash
git add src/components/Gallery.jsx src/data/templates.js
git commit -m "i18n: fully internationalize Gallery; use English category/difficulty keys"
```

---

## Task 7: Update ExportPanel.jsx

**Files:**
- Modify: `src/components/ExportPanel.jsx`

- [ ] **Step 1: Add useTranslation and replace all hardcoded text**

Add import:
```js
import { useTranslation } from 'react-i18next'
```

Add inside component: `const { t } = useTranslation()`

Replace toggle button text:
```jsx
{t('export.title')}
```

Replace export buttons:
```jsx
{t('export.png')}
// ...
{t('export.svg')}
// ...
{t('export.text')}
// ...
{isExporting ? `${t('export.exporting')} ${exportProgress}%` : t('export.patternSheet')}
// ...
{t('export.patternSheetSVG')}
```

- [ ] **Step 2: Commit**
```bash
git add src/components/ExportPanel.jsx
git commit -m "i18n: fully internationalize ExportPanel"
```

---

## Task 8: Update ColorPalette.jsx and ColorStatsBar.jsx

**Files:**
- Modify: `src/components/ColorPalette.jsx`
- Modify: `src/components/ColorStatsBar.jsx`

- [ ] **Step 1: Update ColorPalette.jsx**

Add import at top:
```js
import { useTranslation } from 'react-i18next'
```

Add inside component: `const { t } = useTranslation()`

Replace drawer toggle title:
```jsx
title={collapsed ? t('palette.expand') : t('palette.collapse')}
```

Replace palette header:
```jsx
<h3 className="palette-title">{t('palette.title2')}</h3>
```

- [ ] **Step 2: Update ColorStatsBar.jsx**

Add import at top:
```js
import { useTranslation } from 'react-i18next'
```

Add inside component: `const { t } = useTranslation()`

Replace empty state:
```jsx
<div className="stats-empty-text">{t('stats.noData')}</div>
```

Replace stat labels:
```jsx
<span className="stats-chip-label">{t('stats.beads')}</span>
// ...
<span className="stats-chip-label">{t('stats.colors')}</span>
```

Replace tooltip:
```jsx
title={`${hex} — ${count} ${t('stats.beads')}`}
```

- [ ] **Step 3: Commit**
```bash
git add src/components/ColorPalette.jsx src/components/ColorStatsBar.jsx
git commit -m "i18n: internationalize ColorPalette and ColorStatsBar"
```

---

## Task 9: Update ImageQuantizer.jsx remaining hardcoded strings

**Files:**
- Modify: `src/components/ImageQuantizer/ImageQuantizer.jsx`

- [ ] **Step 1: Remove labels from GRID_PRESETS constant**

Replace the `GRID_PRESETS` array (remove `label` from each entry):
```js
const GRID_PRESETS = [
  { key: '29x29',   w: 29,  h: 29  },
  { key: '57x57',   w: 57,  h: 57  },
  { key: '114x114', w: 114, h: 114 },
  { key: '140x140', w: 140, h: 140 },
  { key: '57x29',   w: 57,  h: 29  },
  { key: '29x57',   w: 29,  h: 57  },
  { key: 'aspect',  w: null, h: null, aspect: true },
  { key: 'custom',  w: null, h: null },
]
```

- [ ] **Step 2: Replace grid preset select options to use t()**

```jsx
{GRID_PRESETS.map(p => (
  <option key={p.key} value={p.key}>{t(`quantizer.presets.${p.key}`)}</option>
))}
```

- [ ] **Step 3: Replace all remaining hardcoded strings in JSX**

Replace aspect mode inputs section:
```jsx
<label style={{ fontSize: 12, color: '#666' }}>
  {t('quantizer.longSide')}: {longSide}
</label>
// ...
<div style={{ fontSize: 12, color: '#888', marginTop: 4 }}>
  {previewUrl ? (
    <>{t('quantizer.actualSize')}: <strong>{gridWidth} × {gridHeight}</strong>（{t('quantizer.aspectRatio')} {imageAspectRatio.toFixed(3)}）</>
  ) : (
    <>{t('quantizer.uploadFirst')}</>
  )}
</div>
```

Replace custom grid inputs placeholders:
```jsx
placeholder={t('quantizer.widthPlaceholder')}
// ...
placeholder={t('quantizer.heightPlaceholder')}
```

Replace maxColors label suffix:
```jsx
{!hasUserTouchedMaxColors && (
  <span style={{ marginLeft: 8, fontSize: 11, color: '#888' }}>
    （{t('quantizer.autoRecommended')}）
  </span>
)}
```

Replace colorHint:
```jsx
<span className="setting-hint">{t('quantizer.colorHint')}</span>
```

Replace algorithm label and dithering options:
```jsx
<label>{t('quantizer.algorithm')}</label>
<select value={dithering} onChange={e => setDithering(e.target.value)} disabled={isProcessing}>
  <option value="none">{t('quantizer.dithering.none')}</option>
  <option value="floyd-steinberg">{t('quantizer.dithering.floydSteinberg')}</option>
  <option value="ordered">{t('quantizer.dithering.ordered')}</option>
</select>
<span className="setting-hint">{t('quantizer.algorithmHint')}</span>
```

Replace quality label and options:
```jsx
<label>{t('quantizer.quality')}</label>
<select value={qualityMode} onChange={e => setQualityMode(e.target.value)} disabled={isProcessing}>
  <option value="high">{t('quantizer.qualityModes.high')}</option>
  <option value="fast">{t('quantizer.qualityModes.fast')}</option>
</select>
<span className="setting-hint">{t('quantizer.qualityHint')}</span>
```

Replace removeBackground label and hint:
```jsx
<span>{t('quantizer.removeBackground')}</span>
// ...
<span className="setting-hint">{t('quantizer.removeBackgroundHint')}</span>
```

Replace brightness/contrast labels:
```jsx
<label>{t('quantizer.brightness')}: {brightness > 0 ? `+${brightness}` : brightness}</label>
// ...
<label>{t('quantizer.contrast')}: {contrast > 0 ? `+${contrast}` : contrast}</label>
```

Replace progress section:
```jsx
<small>{t('quantizer.processingAlgo')}</small>
```

Replace result section warnings and badges:
```jsx
{hasUnsavedChanges && (
  <span className="settings-changed-warning">（{t('quantizer.settingsChanged')}）</span>
)}
// ...
{dithering !== 'none' && <span className="dithering-badge">{t('quantizer.ditheringApplied')}</span>}
{hasUnsavedChanges && (
  <span className="regenerate-hint">{t('quantizer.regenerateHint')}</span>
)}
```

Replace unsaved dialog:
```jsx
<h3>{t('quantizer.unsavedTitle')}</h3>
<p>{t('quantizer.unsavedBody')}</p>
// ...
<button className="btn btn-secondary" onClick={() => { setShowUnsavedDialog(false); pendingCloseRef.current = false }}>
  {t('quantizer.continueEditing')}
</button>
<button className="btn btn-primary" onClick={handleDiscardChanges}>
  {t('quantizer.discardClose')}
</button>
```

- [ ] **Step 4: Commit**
```bash
git add src/components/ImageQuantizer/ImageQuantizer.jsx
git commit -m "i18n: fully internationalize ImageQuantizer"
```

---

## Task 10: Update Tutorials.jsx

**Files:**
- Modify: `src/components/Tutorials.jsx`

- [ ] **Step 1: Add useTranslation import**

Add at top:
```js
import { useTranslation } from 'react-i18next'
```

Add inside `Tutorials` component: `const { t } = useTranslation()`
Add inside `NavigationButtons` component: `const { t } = useTranslation()`

- [ ] **Step 2: Replace all hardcoded UI text in Tutorials component**

```jsx
<h2 className="sidebar-title">{t('tutorials.title')}</h2>
// ...
<button className="progress-btn" onClick={markAllRead}>{t('tutorials.markAllRead')}</button>
<button className="progress-btn reset" onClick={resetProgress}>{t('tutorials.reset')}</button>
```

Replace steps section title:
```jsx
<h3 className="steps-title">
  ...svg...
  {t('tutorials.steps')}
</h3>
```

Replace tips header:
```jsx
<span>{t('tutorials.tips')}</span>
```

Replace diagram placeholder:
```jsx
<span>{t('tutorials.diagram')}</span>
```

Replace no-selection state:
```jsx
<p>{t('tutorials.noSelection')}</p>
```

- [ ] **Step 3: Replace hardcoded text in NavigationButtons**

```jsx
{t('tutorials.prev')}{prevTutorial?.title || t('tutorials.noMore')}
// ...
{t('tutorials.next')}{nextTutorial?.title || t('tutorials.noMore')}
```

- [ ] **Step 4: Commit**
```bash
git add src/components/Tutorials.jsx
git commit -m "i18n: internationalize Tutorials UI chrome"
```

---

## Task 11: Update MobileToolbar.jsx

**Files:**
- Modify: `src/components/Tools/MobileToolbar.jsx`

- [ ] **Step 1: Check MobileToolbar already uses t() for tool labels — verify and add any missing strings**

Read the file. It already imports `useTranslation`. Verify all button labels use `t()`. The tools array should be:
```js
const tools = [
  { id: 'pencil', icon: '✏️', label: t('canvas.tool.pencil') },
  { id: 'eraser', icon: '🧹', label: t('canvas.tool.eraser') },
  { id: 'fill', icon: '🪣', label: t('canvas.tool.fill') },
  { id: 'hand', icon: '🖐️', label: t('canvas.tool.hand') },
]
```
If already correct, no change needed. Check action buttons (undo/clear) and add t() if missing.

- [ ] **Step 2: Commit if changes were needed**
```bash
git add src/components/Tools/MobileToolbar.jsx
git commit -m "i18n: verify/fix MobileToolbar translations"
```
